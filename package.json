{
  "name": "nexus-webrtc-platform",
  "version": "1.0.0",
  "private": true,
  "description": "Full-Stack Real-Time Video, Audio Calling, Chat & Subscription Platform",
  "engines": {
    "node": ">=22.0.0"
  },
  "scripts": {
    "build:server": "npm install --include=dev --prefix server && npm --prefix server run build",
    "build:client": "npm install --include=dev --prefix client && npm --prefix client run build",
    "build": "npm run build:server && npm run build:client",
    "start": "node server/dist/server.js"
  }
}
